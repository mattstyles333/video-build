# Either key is enough for transcription. If both are set, Grok STT is used
# unless you pass --provider elevenlabs.
# XAI_API_KEY is also required for imagine.py (generated stills / B-roll).
XAI_API_KEY=
ELEVENLABS_API_KEY=
